package hr.gkadnar.ch01.identifiers;

public class LegalIdentifiers {

	int :b;		// identifier cannot start with :
	int -d;		// identifier cannot start with -
	int e#1;	// identifier cannot contain # character
	int .f;		// identifier cannot start with .
	int 7g;		// identifier cannot start with a number
}
